repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
        exclude: '^.*\.md$'
      - id: end-of-file-fixer
        exclude: '^.*\.(png|ico|idx)$'
      - id: check-merge-conflict
      - id: check-case-conflict
      - id: check-added-large-files
        args: ['--maxkb=500']
      - id: mixed-line-ending
        args: ['--fix=lf']

  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v22.1.5
    hooks:
      - id: clang-format
        types_or: [c++, c]
        args: ['--style=file']

  - repo: https://github.com/pocc/pre-commit-hooks
    rev: v1.3.5
    hooks:
      - id: cppcheck
        exclude: '\.h$'
        args:
          - '--language=c++'
          - '--std=c++20'
          - '--library=qt'  # Qt macros (slots/signals/Q_OBJECT) in ui/QTest files
          - '--inline-suppr'  # honor narrowly-scoped `// cppcheck-suppress` comments
          - '--enable=warning,style,performance'
          - '--error-exitcode=1'
          - '--quiet'

  - repo: local
    hooks:
      - id: cmake-tests
        name: Run unit tests (pre-push)
        language: system
        entry: scripts/run-tests.sh
        stages: [pre-push]
        pass_filenames: false
        always_run: true
